---
- name: Verify error when unsupported top-level params are provided
  block:
    - name: Execute platform update with unsupported top-level parameter vios_name
      platform_update:
        hmc_host: "{{ inventory_hostname }}"
        hmc_auth: "{{ curr_hmc_auth }}"
        system_name: "{{ system_name }}"
        vios_name: "{{ vios_name }}"
        platform_config:
          system_firmware_update:
            update_type: Update
            update_order: 1
      register: result
      ignore_errors: yes

    - assert:
        that:
          - result is failed
          - result is not changed
          - "'Unsupported' in result.msg"

  rescue:
    - name: Mark the testcase as failed
      debug:
        msg: "{{ Status_Fail }}"
