---
- name: Verify that play reports error when taskrole is missing in attributes dictionary for present state
  block:
    - name: Verify that play reports error when taskrole is missing in attributes dictionary for present state
      hmc_user:
        state: present
        hmc_host: "{{ inventory_hostname }}"
        name: "{{ name }}"
        hmc_auth: "{{ curr_hmc_auth }}"
        attributes:
          resourcerole: "{{ resourcerole }}"
          authentication_type: "{{ authentication_type }}"
          passwd: "{{ passwd }}"
      register: result
      ignore_errors: yes

    - assert:
        that:
          - "'ParameterError: mandatory parameter taskrole is missing' in result.msg"
          - result is failed
          - result is not changed

  rescue:
    - name: Mark the testcase as failed
      debug:
        msg: "{{ status_fail }}"
