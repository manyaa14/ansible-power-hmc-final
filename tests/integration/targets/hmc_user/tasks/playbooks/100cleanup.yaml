- name: Fetch all the VIOS & Lpars
  hmc_command:
    hmc_host: '{{ inventory_hostname }}'
    hmc_auth: "{{ curr_hmc_auth }}"
    cmd: "{{ item }}"
  register: testout
  loop:
    - chhmcldap -o r -r ldap
    - rmhmcusr -t all

