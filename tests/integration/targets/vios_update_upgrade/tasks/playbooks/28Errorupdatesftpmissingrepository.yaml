---
- name: Verify error when repository is missing for SFTP update
  block:
    - name: Update vios without repository type but with SFTP params
      vios_update_upgrade:
        hmc_host: "{{ inventory_hostname }}"
        hmc_auth: "{{ curr_hmc_auth }}"
        attributes:
          system_name: "{{ system_name }}"
          host_name: "{{ sftp_host }}"
          user_id: "{{ sftp_user }}"
          vios_id: "{{ vios_id }}"
        state: updated
      register: result
      ignore_errors: yes

    - assert:
        that:
          - result is failed
          - result is not changed
          - "'repository' in result.msg"

  rescue:
    - name: Mark the testcase as failed
      debug:
        msg: "{{ Status_Fail }}"
