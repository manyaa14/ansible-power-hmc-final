---
- name: Update vios using ibmwebsite with restart and save options
  block:
    - name: Update using ibmwebsite with restart=true and save=true
      vios_update_upgrade:
        hmc_host: "{{ inventory_hostname }}"
        hmc_auth: "{{ curr_hmc_auth }}"
        attributes:
          repository: ibmwebsite
          system_name: "{{ system_name_mtms }}"
          vios_id: "{{ vios_id }}"
          image_name: "{{ image_name }}"
          restart: true
          save: true
        state: updated
      register: result

    - name: Display result
      debug:
        msg: "{{ result }}"

    - assert:
        that:
          - result is success

  rescue:
    - name: Mark the testcase as failed
      debug:
        msg: "{{ Status_Fail }}"
