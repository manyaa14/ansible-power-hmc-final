- name: Error-when max-mem mentioned without mem
  block:
    - name: Error-when max-mem mentioned without mem
      powervm_lpar_instance:
        hmc_host: '{{ inventory_hostname }}'
        hmc_auth: "{{ curr_hmc_auth }}"
        system_name: "{{ system_name }}"
        vm_name: "{{ vm_name  }}"
        os_type: "{{ os_type_aix }}"
        max_mem: "{{ max_mem }}"
        state: "{{ state_present }}"
      register: result
      ignore_errors: yes

    - name: Verify the error message
      assert:
        that:
          - '"{{ error1 }}" in result.msg'
          - result is failed
          - result is not changed

  rescue:
    - name: Mark the testcase as FAILED
      debug:
        msg: "{{ Status_Fail }}"


