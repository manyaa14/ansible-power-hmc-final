- name: Error-when fc_port is missing
  block:
    - name: Error-when fc_port is missing
      powervm_lpar_instance:
        hmc_host: '{{ inventory_hostname }}'
        hmc_auth: "{{ curr_hmc_auth }}"
        system_name: "{{ system_name }}"
        vm_name: "{{ vm_name + 'NPIV' }}"
        os_type: "{{ os_type_ibmi }}"
        max_virtual_slots: "{{ max_virtual_slots }}"
        npiv_config:
          - vios_name: "{{ fc_port }}"
            wwpn_pair: "{{ wwpn_pair }}"
        state: "{{ state_present }}"
      register: result
      ignore_errors: yes

    - name: Stdout the Error details
      debug:
        msg: "{{ result }}"

    - name: Verify the error message
      assert:
        that:
          - result is failed
          - result is not changed
          - "'missing required arguments: fc_port found in npiv_config' in result.msg"

  rescue:
    - name: Mark the testcase as FAILED
      debug:
        msg: "{{ Status_Fail }}"
