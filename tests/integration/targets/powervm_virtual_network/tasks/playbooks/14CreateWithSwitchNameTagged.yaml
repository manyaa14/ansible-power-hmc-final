- name: Create virtual network with switch_name tagged
  block:
    - name: Create virtual network using switch_name with tagged_network true
      powervm_virtual_network:
        hmc_host: '{{ inventory_hostname }}'
        hmc_auth: "{{ curr_hmc_auth }}"
        system_name: "{{ system_name }}"
        network_name: "{{ network_name_create_tagged }}"
        network_vlan_id: "{{ network_vlan_id_tagged }}"
        switch_name: "{{ switch_name }}"
        tagged_network: "{{ tagged_network_true }}"
        state: present
      register: testout

    - name: Stdout the create tagged response
      debug:
        msg: '{{ testout }}'

    - name: Verify tagged virtual network is created
      assert:
        that:
          - testout is changed
          - testout is success

  rescue:
    - name: Mark the testcase as FAILED
      debug:
        msg: "{{ Status_Fail }}"
