- name: Verify minimal dedicated profile creation succeeds
  block:
    - name: Create a dedicated partition profile with required parameters
      powervm_partition_profile:
        hmc_host: '{{ inventory_hostname }}'
        hmc_auth: "{{ curr_hmc_auth }}"
        system_name: "{{ system_name }}"
        vm_name: "{{ vm_name }}"
        name: "{{ profile_dedicated_basic }}"
        state: present
        memory_settings: "{{ memory_settings_basic }}"
        processor_settings: "{{ dedicated_processor_settings_basic }}"
      register: testout

    - name: Stdout the dedicated profile creation response
      debug:
        msg: '{{ testout }}'

    - name: Verify dedicated profile is created successfully
      assert:
        that:
          - testout is success
          - testout is changed
          - "'partition profile is created successfully' in testout.profile_info.msg"

  rescue:
    - name: Mark the testcase as FAILED
      debug:
        msg: "{{ Status_Fail }}"

# Made with Bob