- name: Verify profile creation succeeds with explicit expansion factor
  block:
    - name: Create shared partition profile with explicit memory expansion factor
      powervm_partition_profile:
        hmc_host: '{{ inventory_hostname }}'
        hmc_auth: "{{ curr_hmc_auth }}"
        system_name: "{{ system_name }}"
        vm_name: "{{ vm_name }}"
        name: "{{ profile_memory_expansion }}"
        state: present
        memory_settings: "{{ memory_settings_ame_explicit }}"
        processor_settings: "{{ shared_processor_settings_pool }}"
      register: testout

    - name: Stdout the explicit expansion factor response
      debug:
        msg: '{{ testout }}'

    - name: Verify profile is created successfully with explicit expansion factor
      assert:
        that:
          - testout is success
          - testout is changed
          - "'partition profile is created successfully' in testout.profile_info.msg"

  rescue:
    - name: Mark the testcase as FAILED
      debug:
        msg: "{{ Status_Fail }}"

# Made with Bob
