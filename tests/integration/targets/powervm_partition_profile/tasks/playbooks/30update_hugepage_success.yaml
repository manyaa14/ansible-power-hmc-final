- name: Verify update succeeds when huge page settings are changed
  block:
    - name: Update shared partition profile huge page settings
      powervm_partition_profile:
        hmc_host: '{{ inventory_hostname }}'
        hmc_auth: "{{ curr_hmc_auth }}"
        system_name: "{{ system_name }}"
        vm_name: "{{ vm_name }}"
        name: "{{ profile_update_shared }}"
        state: updated
        memory_settings: "{{ memory_settings_update_shared_hugepages }}"
      register: testout

    - name: Stdout the update hugepage response
      debug:
        msg: '{{ testout }}'

    - name: Verify huge page update succeeds
      assert:
        that:
          - testout is success
          - testout is changed
          - "'partition profile is updated successfully' in testout.profile_info.msg"

  rescue:
    - name: Mark the testcase as FAILED
      debug:
        msg: "{{ Status_Fail }}"

# Made with Bob
