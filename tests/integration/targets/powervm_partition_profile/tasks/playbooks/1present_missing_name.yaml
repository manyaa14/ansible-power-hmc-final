- name: Verify create fails when profile name is missing
  block:
    - name: Create partition profile without required name
      powervm_partition_profile:
        hmc_host: '{{ inventory_hostname }}'
        hmc_auth: "{{ curr_hmc_auth }}"
        system_name: "{{ system_name }}"
        vm_name: "{{ vm_name }}"
        state: present
      register: testout
      ignore_errors: true

    - name: Stdout the missing-name response
      debug:
        msg: '{{ testout }}'

    - name: Verify missing profile name error is reported
      assert:
        that:
          - testout is failed
          - testout is not changed
          - "'missing required arguments: name' in testout.msg"
          - "'name' in testout.msg"

  rescue:
    - name: Mark the testcase as FAILED
      debug:
        msg: "{{ Status_Fail }}"

# Made with Bob
