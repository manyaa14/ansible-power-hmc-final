---
- name: "TC24 - Error: update_npiv with missing system_name"
  block:
    - name: Run powervm_dlpar update_npiv without system_name
      powervm_dlpar:
        hmc_host: "{{ inventory_hostname }}"
        hmc_auth: "{{ curr_hmc_auth }}"
        system_name: ""
        vm_name: "{{ vm_name }}"
        npiv_settings:
          - vios_name: "{{ vios_name }}"
            fc_port_name: "{{ fc_port_name }}"
        action: update_npiv
      register: result
      ignore_errors: true

    - name: Verify task failed with missing system_name error
      assert:
        that:
          - result is failed
          - result is not changed
          - "'system_name' in result.msg"
        fail_msg: "{{ Status_Fail }}"
  rescue:
    - name: Testcase failed
      debug:
        msg: "{{ Status_Fail }}"
