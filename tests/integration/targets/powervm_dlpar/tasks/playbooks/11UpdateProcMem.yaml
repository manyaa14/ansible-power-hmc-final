---
- name: "TC11 - Update both proc and mem on partition"
  block:
    - name: Run powervm_dlpar update_proc_mem with proc and mem settings
      powervm_dlpar:
        hmc_host: "{{ inventory_hostname }}"
        hmc_auth: "{{ curr_hmc_auth }}"
        system_name: "{{ system_name }}"
        vm_name: "{{ vm_name }}"
        proc_settings:
          proc_unit: "{{ proc_unit }}"
          proc: "{{ proc }}"
        mem_settings:
          mem: "{{ mem }}"
        action: update_proc_mem
      register: result

    - name: Verify task succeeded with changed status
      assert:
        that:
          - result is success
          - result.partition_info is defined
        fail_msg: "{{ Status_Fail }}"
  rescue:
    - name: Testcase failed
      debug:
        msg: "{{ Status_Fail }}"
