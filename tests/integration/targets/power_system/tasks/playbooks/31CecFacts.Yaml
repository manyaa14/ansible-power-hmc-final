- name: Retrieve Cec Facts
  block:
    - name: Verify cec facts
      power_system:
        hmc_host: '{{ inventory_hostname }}'
        hmc_auth: "{{ curr_hmc_auth }}"
        system_name: "{{ system_name }}"
        state: "{{ state }}"
      register: result
    
    - name: Validate the result
      assert:
        that:
          - result is success
          - result is not changed

  rescue:
    - name: Mark the Testcase as FAILED
      debug:
        msg: "{{ Status_Fail }}"
