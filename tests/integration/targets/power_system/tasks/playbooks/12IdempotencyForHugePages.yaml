- name: Modify HugePages
  block:
    - name: Verify that play follows idempotency when user tries to change huge page to existing huge page count
      power_system:
        hmc_host: '{{ inventory_hostname }}'
        hmc_auth: "{{ curr_hmc_auth }}"
        system_name: "{{ system_name }}"
        requested_num_sys_huge_pages: "{{ requested_num_sys_huge_pages }}"
        action: "{{ action_modify_hwres }}"
      register: result

    - name: Verify the result
      assert:
        that:
          - result is success
          - result is not changed

  rescue:
    - name: Mark the testcase as FAILED
      debug:
        msg: "{{ Status_Fail }}"


       

