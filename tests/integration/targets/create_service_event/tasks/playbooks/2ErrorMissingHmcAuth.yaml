---
- name: "TC2 - Error: missing hmc_auth"
  block:
    - name: Run create_service_event without hmc_auth
      create_service_event:
        hmc_host: "{{ inventory_hostname }}"
        system_name: "{{ system_name }}"
        description: "Test Description"
        types: vios
        attributes:
          title: "{{ svc_title }}"
          severity: "{{ svc_severity }}"
          contact_name: "{{ svc_contact_name }}"
          contact_phone: "{{ svc_contact_phone }}"
          contact_email: "{{ svc_contact_email }}"
          lpar_name: "{{ vios_name }}"
          service_file:
            - pedbgq4
        state: created
      register: result
      ignore_errors: true

    - name: Verify task failed with missing hmc_auth error
      assert:
        that:
          - result is failed
          - result is not changed
          - "'hmc_auth' in result.msg"
        fail_msg: "{{ Status_Fail }}"
  rescue:
    - name: Testcase failed
      debug:
        msg: "{{ Status_Fail }}"
