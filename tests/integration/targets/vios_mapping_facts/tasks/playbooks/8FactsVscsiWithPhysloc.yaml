---
- name: "TC8 - Facts: vscsi with physloc filter"
  block:
    - name: Run vios_mapping_facts with component vscsi and physloc
      vios_mapping_facts:
        hmc_host: "{{ inventory_hostname }}"
        hmc_auth: "{{ curr_hmc_auth }}"
        system_name: "{{ system_name }}"
        vios_name: "{{ vios_name }}"
        component: vscsi
        physloc: "{{ physloc_vscsi }}"
        state: facts
      register: result

    - name: Verify task succeeded and physloc-filtered vscsi mapping returned
      assert:
        that:
          - result is success
          - result is not changed
          - result.mapping_facts is defined or result.warning is defined
        fail_msg: "{{ Status_Fail }}"
  rescue:
    - name: Testcase failed
      debug:
        msg: "{{ Status_Fail }}"
